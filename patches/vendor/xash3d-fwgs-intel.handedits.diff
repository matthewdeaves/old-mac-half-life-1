diff --git a/engine/platform/sdl2/sys_sdl2.c b/engine/platform/sdl2/sys_sdl2.c
index 88409f31..aa60400d 100644
--- a/engine/platform/sdl2/sys_sdl2.c
+++ b/engine/platform/sdl2/sys_sdl2.c
@@ -115,8 +115,12 @@ void SDLash_Init( void )
 	// SDL_SetHint( SDL_HINT_WINDOWS_DPI_SCALING, "1" );
 #endif // XASH_WIN32
 
+#ifdef SDL_HINT_ANDROID_BLOCK_ON_PAUSE // added in SDL 2.0.12; legacy macOS SDL2 (2.0.6) lacks it
 	SDL_SetHint( SDL_HINT_ANDROID_BLOCK_ON_PAUSE, "0" );
+#endif
+#ifdef SDL_HINT_ANDROID_BLOCK_ON_PAUSE_PAUSEAUDIO // added in SDL 2.0.16
 	SDL_SetHint( SDL_HINT_ANDROID_BLOCK_ON_PAUSE_PAUSEAUDIO, "0" );
+#endif
 
 	// when launched through Steam (notably on Steam Deck) Steam Input hides the
 	// real controller and exposes a virtual gamepad without gyro/touchpad access
@@ -134,8 +138,12 @@ void SDLash_Init( void )
 	}
 
 	SDL_SetHint( SDL_HINT_ACCELEROMETER_AS_JOYSTICK, "0" );
+#ifdef SDL_HINT_JOYSTICK_HIDAPI_STEAM // added in SDL 2.0.14
 	SDL_SetHint( SDL_HINT_JOYSTICK_HIDAPI_STEAM, "1" );
+#endif
+#ifdef SDL_HINT_ANDROID_TRAP_BACK_BUTTON // added in SDL 2.0.9
 	SDL_SetHint( SDL_HINT_ANDROID_TRAP_BACK_BUTTON, "1" );
+#endif
 	SDL_SetHint( SDL_HINT_ORIENTATIONS, "LandscapeLeft LandscapeRight" );
 
 #ifdef SDL_HINT_MOUSE_TOUCH_EVENTS
